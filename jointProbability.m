function jp=jointProbability(samples,classes)
    states={'0','1','2+'};
    jp=zeros(length(classes),length(classes),3,3); %jp(i,j,state_i,state_j)

    for i=1:length(samples)
        samples(i)
        
    end


end